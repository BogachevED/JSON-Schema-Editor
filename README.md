# JSON-Schema-Editor
The application was created to work with JSON Schema. It consists of a client and a server part. The server part is made using Node.JS, and the client version is made using Vue 3.
